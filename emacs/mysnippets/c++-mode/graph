# -*- mode: snippet -*-
# name: graph
# key: graph
# --
int N, M;
cin >> N >> M;
vector<vector<int> > graph(N);
for (int m = 0; m < M; m++) {
  int U, V;
  cin >> U >> V;
  U--; V--;
  graph.at(U).push_back(V);
  //graph.at(V).push_back(U);
}
