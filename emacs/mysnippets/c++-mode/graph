# -*- mode: snippet -*-
# name: graph
# key: graph
# --
int N, M;
cin >> N >> M;
vector<vector<int> > graph(N);
for (int j = 0; j < ${1:M}; j++) {
  int U, V;
  cin >> U >> V;
  U--; V--;
  graph.at(U).push_back(V);
  //graph.at(V).push_back(U);
}
