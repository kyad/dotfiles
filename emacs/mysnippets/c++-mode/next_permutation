# -*- mode: snippet -*-
# name: next_permutation
# key: next_permutation
# --
// 順列初期化
vector<int> P(N, -1);
for (int i = 0; i < N; i++) {
  P.at(i) = i;
}
// 順列全探索
do {
  for (int i = 0; i < N; i++) {
    int p = P.at(i);
    $0
  }
} while (next_permutation(P.begin(), P.end()));
