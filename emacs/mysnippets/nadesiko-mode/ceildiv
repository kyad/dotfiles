# -*- mode: snippet -*-
# name: ceildiv
# key: ceildiv
# --
●(aをbで)floordivとは
# floor(a / b)を計算する。aが負の場合は負の無限大へ丸める。
もし、a>=0ならば、
  a/bを整数変換して答えに代入する
違えば、
  -aをbでceildiv
  答えはそれ*-1
ここまで。
答えを戻す。
ここまで。

●(aをbで)ceildivとは
# ceil(a / b)を計算する。aが負の場合は正の無限大へ丸める。
もし、a>=0ならば、
  (a+b-1)/bを整数変換して答えに代入する
違えば、
  -aをbでfloordiv
  答えはそれ*-1
ここまで。
答えを戻す。
ここまで。
